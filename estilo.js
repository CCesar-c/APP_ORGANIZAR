
const inputs = {

    color: "black",
    height: 30,
    borderRadius: 5,
    borderColor: "black",
    borderWidth: 1
}

const titulo = {
    fontSize: 30,
    fontWeight: 1000
}
const boton = {
    borderRadius: 5,
    height: 50,
    with: 100,
    borderColor: "black",
    color: "black",
    borderWidth: 1
}

const container = {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    // justifyContent: 'center',
}

export { titulo, boton, container, inputs }